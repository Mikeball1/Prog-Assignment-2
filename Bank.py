#Self intializes variables and assigns value based on future inputs
class bank:
  def __init__(self,savingsbal,name,accnum,chequingbal):
   self.savingsbal=savingsbal
   self.name=name
   self.accnum=accnum
   self.chequingbal=chequingbal